/* eslint-disable camelcase */
export type Instrument = {
  buy_price: string;
  sell_price: string;
  last_trade: string;
  high: string;
  low: string;
  avg: string;
  vol: string;
  vol_curr: string;
  updated: number;
};
